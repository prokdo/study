import model.random.DataGenerator
import model.util.TaskInfo
import view.terminal.Terminal

fun main() {
//    DataGenerator.generate(TaskInfo.RSRC_PATH, TaskInfo.FILES_NUMBER, TaskInfo.USERS_NUMBER)
    Terminal.launch()
}